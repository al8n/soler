use logosky::keyword;

keyword! {
  /// The contextual `from` keyword in Solidity
  (From, "FROM", "from"),
  /// The contextual `error` keyword in Solidity
  (Error, "ERROR", "error"),
  /// The contextual `revert` keyword in Solidity
  (Revert, "REVERT", "revert"),
  /// The contextual `global` keyword in Solidity
  (Global, "GLOBAL", "global"),
  /// The contextual `transient` keyword in Solidity
  (Transient, "TRANSIENT", "transient"),
  /// The contextual `layout` keyword in Solidity
  (Layout, "LAYOUT", "layout"),
  /// The contextual `at` keyword in Solidity
  (At, "AT", "at"),
}
